<?xml version="1.0"?>
<launch>
  <!-- Arguments -->
  <arg name="open_rviz" default="true"/>

  <!-- rviz -->
  <group if="$(arg open_rviz)">
    <node pkg="rviz" type="rviz" name="rviz_master" required="true"
      args="-d $(dirname)/navigation_master.rviz"/>
  </group>
</launch>
